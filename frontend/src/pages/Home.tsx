import Typography from '@mui/material/Typography'; // Import Typography from MUI

const Home = () => {
    return (
        <div>
            <Typography variant="h6" color='text.primary'>
                Pagina Home de Eduardo Viera
            </Typography>
        </div>
    );
};

export default Home;
